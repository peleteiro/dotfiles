---
# Power settings for an always-on headless macmini (applied via `pmset -a`).
nosleep_pmset:
  sleep: 0 # never sleep (system sleep) — the main one
  disksleep: 0 # disk never sleeps
  powernap: 0 # no Power Nap
  womp: 1 # wake on network access (wakes on a network packet)
