---
# Dispatch by OS — the same role ("docker installed, updated and running") is fulfilled
# differently on each platform. macmini = Darwin (Colima). When the Home
# Assistant (Linux) needs docker, add a Debian.yml here.
- name: Install/ensure Docker according to the OS
  ansible.builtin.include_tasks: "{{ ansible_facts['os_family'] }}.yml"
