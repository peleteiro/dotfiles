---
# Dispatch by OS — bash/curl/rust-cli are installed via Homebrew (macOS) or apt (Linux).
- name: Install base tools according to the OS
  ansible.builtin.include_tasks: "{{ ansible_facts['os_family'] }}.yml"
